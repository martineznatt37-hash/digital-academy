{
  "name": "digital-academy",
  "version": "1.0.0",
  "private": true,
  "description": "Digital Academy — root package for Render deploy",
  "scripts": {
    "start": "node server/server.js",
    "build": "npm install --prefix server",
    "postinstall": "npm install --prefix server"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
